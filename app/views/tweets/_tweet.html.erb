<div class="mb-3 card card-body">
	<%= tweet.body %>
	<div class="me-4">
		<%= image_tag tweet.twitter_account.image, class: "rounded-circle" %>
		<%= link_to "@#{tweet.twitter_account.username}", "https://twitter.com/#{tweet.twitter_account.username}", target: :_blank %>

		<% if tweet.published? %>
		<%= link_to "view Tweet", "https://twitter.com/#{tweet.twitter_account.username}/status/#{tweet.id}", target: :_blank %>
		<% else %>
			Scheduled on: <%= l tweet.publish_at, format: :short %> UTC
			<%= link_to "Edit Tweet", edit_tweet_path(tweet) %>
		<% end %>

	</div>
</div>